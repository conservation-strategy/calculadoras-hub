import { Gudea, Duru_Sans, Doppio_One, } from 'next/font/google';

export const gudea = Gudea({ subsets: ['latin'], weight: ['400', '700'] });
export const duru_sans = Duru_Sans({ subsets: ['latin'], weight: '400' });
export const doppio_one = Doppio_One({ subsets: ['latin'] , weight: '400' });

