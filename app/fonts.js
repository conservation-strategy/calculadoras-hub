import { Gudea, Duru_Sans, Doppio_One, } from 'next/font/google';

export const gudea = Gudea({ subsets: ['latin'] });
export const duru_sans = Duru_Sans({ subsets: ['latin'] });
export const doppio_one = Doppio_One({ subsets: ['latin'] });

